[package]
name = "propchain-bridge"
version = "1.0.0"
authors = ["PropChain Team <dev@propchain.io>"]
edition = "2021"

[dependencies]
soroban-sdk = { workspace = true }

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"
